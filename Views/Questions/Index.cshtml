@model IEnumerable<Mental_Capacity_Assessments.Models.Questions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.yes_button)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.no_button)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.yes_next)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.no_next)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.yes_button)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.no_button)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.yes_next)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.no_next)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
